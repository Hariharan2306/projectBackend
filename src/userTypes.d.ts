export type CreateUserData = {
  userName: string;
  mobile: string;
  location: string;
  email: string;
  reciever: boolean;
  registeredId: string;
};
export type LoginUserData = { userMail: string; password: string };
